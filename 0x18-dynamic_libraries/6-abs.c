#include "main.h"

/**
 * _abs - compute absolute value of an integer
 * Return: absolute interger
 *@n : interger value to be converted to absolute number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
		return (n);
}
